<% content_for :title, "Duplicare angajament #{@commitment.full_identifier}" %>

<h1>Duplică un angajament existent</h1>

<p><%= link_to 'Înapoi la registrul de angajamente', commitments_path, class: 'btn btn-light' %></p>

<%= render partial: 'flashes' %>

<%= render partial: 'form' %>
